services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vdiff
      POSTGRES_PASSWORD: vdiff
      POSTGRES_DB: vdiff
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
